language: java
jdk:
  - openjdk8
script:
  - mvn clean package
cache:
  directories:
    - $HOME/.m2
deploy:
  provider: heroku
  app: tinder-step-danit
  branches:
    only:
      - master
  api_key:
    secure: $HEROKU_API_KEY
